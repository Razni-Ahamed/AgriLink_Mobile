<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only (see src/debug/AndroidManifest.xml). Cleartext HTTP is allowed just for
     the addresses a local backend is reached at; everything else stays HTTPS-only. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- The host PC, as seen from the Android emulator. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
